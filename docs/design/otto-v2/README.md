# Otto v2 — UI mockups (source of truth for the redesign)

Provided by the owner on 2026-09-14 (originally in
`Downloads/Purchase feature redesign/Ottos`); referenced as M1–M5 by
`tasks/done/otto-intelligence-integration.md`.

| File | What it shows |
|---|---|
| `m1-apps-shelf.png` | Apps shelf with the dashed "New screen — with OTTO" tile next to the marketplace tiles |
| `m2-builder-empty.png` | Builder page, empty state: OTTO chat rail (steps Chat/Plan/Approve/Build), STATUS panel with figure, canvas with starter chips |
| `m3-plan.png` | The structured plan card in the rail: SCREEN / SOURCES / FILTERS / COLUMNS as chip rows; status checklist with per-step timings |
| `m4-building.png` | Build in progress: canvas stage list (Reading the plan → Querying the data → Composing the screen → Validating totals), per-block ticks, global "BUILDING SCREEN 24%" pill in the top nav |
| `m5-ready.png` | Built screen rendered natively (KPI cards, filters, table on real ZolStock data), "Save to Apps" actions, manifest caveat as a note line |

The figure in these mockups (grey sphere) was superseded on the same day by
the approved animated **Orb** (`OttoFigure` React component) — see §12 Q2 of
the task file.
