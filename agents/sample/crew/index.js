/**
 * Sample Agent Crew Members
 *
 * Export all crew members for the sample agent.
 * The crew service will load these automatically.
 */

const SampleDefaultCrew = require('./default.crew');

module.exports = {
  SampleDefaultCrew
};
