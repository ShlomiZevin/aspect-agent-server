# Cloud SQL Configuration Template
# Copy this file to cloud-sql-config.env and fill in your values
# Usage: ./provision-cloudsql.sh cloud-sql-config.env

# GCP Project Configuration
GCP_PROJECT_ID=your-project-id

# Region (e.g., europe-west1, us-central1, asia-southeast1)
REGION=europe-west1

# Instance Configuration
INSTANCE_NAME=aspect-agents-db

# Database Version (POSTGRES_14, POSTGRES_15, POSTGRES_16)
DB_VERSION=POSTGRES_15

# Machine Tier
# Development: db-f1-micro (shared CPU, 0.6GB RAM) - cheapest
# Small: db-g1-small (shared CPU, 1.7GB RAM)
# Production: db-n1-standard-1 (1 vCPU, 3.75GB RAM)
# Production+: db-n1-standard-2 (2 vCPU, 7.5GB RAM)
TIER=db-f1-micro

# Database Configuration
DB_NAME=agents_platform_db
DB_USER=agent_admin

# Storage (in GB)
STORAGE_SIZE=10

# Backup Configuration (true/false)
BACKUP_ENABLED=true

# High Availability (true/false)
# Note: Regional HA significantly increases costs
HIGH_AVAILABILITY=false
